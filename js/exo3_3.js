const readlineSync = require("readline-sync");

let arr = [25, 50, 100];  
let duplicate = [];
 
        // for (i = 0; i < arr.length; i++)
        // {
        //    duplicate = duplicate+arr[i];
        // }
        // console.log(duplicate);

        duplicate.push(25, 50, 100);
        console.log(duplicate);
        